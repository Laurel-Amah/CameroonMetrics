export type QuoteRow = {
  pair: string;
  bid: string;
  ask: string;
  changePct: string;
  note: string;
};

export type CommodityRow = {
  name: string;
  unit: string;
  last: string;
  session: string;
};
